@model AvatarFormModel
@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager


<div class="outBox">
    <form>
        <input type="image" height="120" width=" 120" src="@Model.AvatarPixURL" asp-controller="Avatar" asp-action="SelectAvatar" asp-route-id="@Model.Id">
    </form>   
</div>
